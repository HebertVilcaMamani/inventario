/// <reference path="../lib.d.ts" />
import express = require('express');
export interface IOption {
    encoding?: string;
    uploadDir?: string;
    keepExtensions?: boolean;
    maxFieldsSize?: number;
    maxFields?: number;
    hash?: boolean;
    multiples?: boolean;
}
export declare type Middleware = (option?: IOption) => express.RequestHandler;
export declare const parse: Middleware;
